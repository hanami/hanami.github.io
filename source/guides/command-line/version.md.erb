---
title: "Hanami | Guides - Command Line: Version"
---

# Command Line

## Version

By running `hanami version` we can see the current version of the framework that we are using.

```shell
% bundle exec hanami version
v<%= hanami_version %>
```

Besides there are alias `--version` and `-v`.

```shell
% bundle exec hanami --version
v<%= hanami_version %>

% bundle exec hanami -v
v<%= hanami_version %>
```